# Terraform Modules and Environments

- `modules/`: Reusable Terraform components.
- `environments/`: Provider-specific configurations.

Uses local state by default. 
**TODO:** Migrate to HCP Terraform remote state.
