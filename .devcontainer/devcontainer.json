{
    "name": "agoge-forger Python 3.12 + Rust 1.97",
    "build": {
        "dockerfile": "Dockerfile"
    },
    "postCreateCommand": "uv venv && uv pip install -e \".[dev]\" && (cd rust-tools && cargo fetch)",
    "containerUser": "vscode",
    "remoteUser": "vscode",
    "features": {
        "ghcr.io/devcontainers/features/nvidia-cuda:2": {
            "cudaVersion": "12.4",
            "installCudnn": true,
            "installToolkit": true
        }
    },
    "runArgs": [
        "--gpus",
        "all"
    ],
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-python.python",
                "charliermarsh.ruff",
                "ms-python.mypy-type-checker",
                "rust-lang.rust-analyzer",
                "tamasfe.even-better-toml"
            ]
        }
    }
}
